import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'dms-delivery-tracking',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
